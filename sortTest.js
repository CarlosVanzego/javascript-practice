// Sort Function
let numbers = [4,3,7,8,5];

// Ascending order
numbers.sort((a,b) => a - b); 

// Descending order
numbers.sort((a,b) => b - a); 

console.log(numbers)

// --------------------------------------
function sortIt(arr){

let copy =  arr.slice();
let result = [];

 for  (let i = 0; i < arr.length; i++){
   
    }
  }




// Steps for my sort it function that sorts in ascending order

// Step 1:
// Find the length of the array

// Step 2:
// Create a container to store the sorted elements 

// Step 3:
// Step(iterate) through the array

// Step 4:
// Compare the current value to the next value in the array using the less than operand

// Step 5:
// If the current value is less than the next value than the value is stored in the sorted array

// Step 6:
// If the current value is greater than the next value, the current value is not stored in the sorted array

// Step 7:
// Move on to the next value of the array

// Step 8:
// Return the newly sorted array




// Example array sorting from 9/9/ Meeting:
// [7,4,3]

// Make an empty array to store the sorted array.
// Find the array length (3).
// Iterate through the array (set current value equal to the first item of the area)
// Find the current element at index 0 (7), and compare to the next element at index 1 of the array (4),do this by comparing them using the less than opperand to sort the elements in ascending order. [4 is less than 7 so it will go before 7 in the sorted array] and then store the sorted values in ascending order, in the empty array.
// 




// If the number is less than 7 then let the array equal '[number, 7]'
// Ex.
// Array [4,7]
// if (arr[i] < 7){
//   let arr = [arr[i], 7];
// };