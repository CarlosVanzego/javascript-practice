// Anatomy of a function:

    // 1. Key word "function"
    // 2. Declare a function with a name
    // 3. Parentheses, which can take any number or parameters
    // 4. Brackets, code that is scopred for cuntion id contained within


// declaring a variable 
let numbers;

// defining a variable whichs is assignning a value to a variable \
number = [1, 2,43, 34]

// defining the sum value with the parameters 'a' and 'b'
function sum(a, b) {
  return a + b;
}

// console.log(sum(33));

 

// function that takes one number and multiplies it by another number

function multiply(a, b) {
  return a * b;
}

multiply(2, 4)

// Example of doing work inside of the parameters
console.log(multiply(2 * 6, 2))


